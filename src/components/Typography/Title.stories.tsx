import React from "react";
import Typography from "@material-ui/core/Typography";

export default {
  title: "Typography",
  component: Typography
};

export const Title = () => {
  return <Typography variant="h5">Заголовок страницы</Typography>;
};
