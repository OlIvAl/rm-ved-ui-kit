import React from "react";
import Typography from "@material-ui/core/Typography";

export default {
  title: "Typography",
  component: Typography
};

export const Subtitle = () => {
  return <Typography variant="subtitle1">Подзаголовки</Typography>;
};
